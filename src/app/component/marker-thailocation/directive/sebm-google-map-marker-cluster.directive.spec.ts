import { SebmGoogleMapMarkerClusterDirective } from './sebm-google-map-marker-cluster.directive';

describe('SebmGoogleMapMarkerClusterDirective', () => {
  it('should create an instance', () => {
    const directive = new SebmGoogleMapMarkerClusterDirective();
    expect(directive).toBeTruthy();
  });
});
