import { SebmGoogleHeatMapDirectiveDirective } from './sebm-google-heat-map-directive.directive';

describe('SebmGoogleHeatMapDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SebmGoogleHeatMapDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
